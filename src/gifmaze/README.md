Run the `example*.py` scripts (without arguments), then enjoy the generated gif images :smile:.

> **Requirements**: `pillow` and `tqdm`.

See the doc [here](https://neozhaoliang-github-io.vercel.app/gifmaze).
